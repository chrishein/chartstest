<%= form_for(@data_topic) do |f| %>
  <% if @data_topic.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@data_topic.errors.count, "error") %> prohibited this data_topic from being saved:</h2>

      <ul>
      <% @data_topic.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
